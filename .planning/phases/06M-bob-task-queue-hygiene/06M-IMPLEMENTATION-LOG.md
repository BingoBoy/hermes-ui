# Phase 6M: Implementation Log

## 2026-06-04

- Added `assigneeDisplayLabel(task)` and `assigneePillClass(task)` to dashboard JS.
- Added an `Assignee` column to the Bob-oppgaver table.
- Ready tasks without assignee now show `Legacy unassigned`.
- No backend code, routes, or write actions changed.
